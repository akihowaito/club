# 吃一碗电竞 · 月兔寻饼 V11 — 背景 A / 紫黄绿按钮

本版本使用背景 A（dANa / mmexport1790085687268.gif），继承 V10 的 100 个兑换口令、转盘、票据和音乐功能。只更新全局按钮配色与交互状态。

## GitHub Pages 上传

将本包中的 `index.html` 直接上传并覆盖目标仓库对应活动目录的 `index.html`，提交 Commit changes。请勿上传 ZIP 本身或多套文件夹。

提交后在仓库 `index.html` 中搜索 `V11-A-purple-yellow-green`，确认该版本已经上传。浏览器刷新页面后查看结果。

素材通过外部图床加载，背景 GIF 的速度和可用性取决于图床与访问网络。该版本依然属于静态演示：前端口令与中奖记录不能提供跨设备防重或防伪。
